const sayHi = (name) => {
    console.log(`Hello there ${name}`)
  }
  // export default
  module.exports = sayHi
  // Here we can export function just by invoking the function in chi 
sayHi('Atharva')